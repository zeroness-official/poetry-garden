# 🍲 village (ui / 心)

ここは `poetry-garden` の `ui`（United / 統合）層であり、「ウタゲ（心）」のメタファー を担当します。

## 役割

`garden`（NI層） で収穫（`harvest`） された原理（`zeroness`）や理論（`aunix`）を、コミュニティ（ファン）が「加工（`prepare`）」 し、「振る舞い（`feast`）」、議論するための「場」です。

(現在はプレースホルダーです。将来的にコミュニティ機能が実装されます。)
