# 🌍 city (ai / 人生)

ここは `poetry-garden` の `ai`（Application / 応用）層であり、「タビ（人生）」のメタファー を担当します。

## 役割

`garden`（NI層） と `village`（UI層） で完成した理論を、一般ユーザー（心の旅人）向けに「普及・実践」する「場」です。

あなたの「心の旅」はここから始まります（`departure`）。

(現在はプレースホルダーです。将来的に実践コミュニティ機能が実装されます。)
